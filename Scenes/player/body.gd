extends Sprite2D

#i coulnt get this to work

#var myPos : Vector2
#var headPos = Main.snakeData[0]
## Called when the node enters the scene tree for the first time.
#func _ready() -> void:
	#pass # Replace with function body.
#
#
## Called every frame. 'delta' is the elapsed time since the previous frame.
#func _process(delta: float) -> void:
	#myPos = get_parent().position / 10
	#myPos.x -= 1
	#headPos = Main.snakeData[-1]
	##print("head pos: ", headPos)
	##print("myPos: ", myPos)
	#
	#if myPos == headPos:
		#hide()
	#else:
		#show()
